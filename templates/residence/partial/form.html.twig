{{ form_start(form) }}
{{ form_widget(form) }}
<div class="form-group">
    <div class="col-sm-2"></div>
    <div class="col-sm-10">
        <input type="submit" value="Save" class="btn btn-primary" />
        {% if residence.id is null %}
            <a href="{{ path('residence_index') }}" class="btn btn-warning"> Cancel </a>
        {% else %}
            <a href="{{ path('residence_show', { 'id': residence.id }) }}" class="btn btn-warning"> Cancel </a>
        {% endif %}
    </div>
</div>
{{ form_end(form) }}

