<table class="table table-bordered table-condensed table-hover table-striped">
    <thead>
        <tr>
            <th>ID</th>
            <th>Name</th>
            <th>Label</th>
            <th>Description</th>

        </tr>
    </thead>
    <tbody>
        {% for relationship_category in relationship_categories %}
            <tr>
                <td>
                    <a href="{{ path('relationship_category_show', { 'id': relationship_category.id }) }}">
                        {{ relationship_category.id }}
                    </a>
                </td>
                <td>
                    {{ relationship_category.name }}
                </td>

                <td>
                    {{ relationship_category.label }}
                </td>

                <td>
                    {{ relationship_category.description|raw }}
                </td>

            </tr>
        {% endfor %}
    </tbody>
</table>
