<table class="table table-bordered table-condensed table-hover table-striped">
    <thead>
        <tr>
            <th>ID</th>
            <th>Name</th>
            <th>Label</th>
            <th>Description</th>

        </tr>
    </thead>
    <tbody>
        {% for location_category in location_categories %}
            <tr>
                <td>
                    <a href="{{ path('location_category_show', { 'id': location_category.id }) }}">
                        {{ location_category.id }}
                    </a>
                </td>
                <td>
                    {{ location_category.name }}
                </td>

                <td>
                    {{ location_category.label }}
                </td>

                <td>
                    {{ location_category.description|raw }}
                </td>

            </tr>
        {% endfor %}
    </tbody>
</table>
