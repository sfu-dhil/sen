{% extends 'base.html.twig' %}

{% block title %}Person Edit{% endblock %}

{% block body %}
    <h1>Person Edit</h1>

    {{ form_start(edit_form) }}
    {{ form_widget(edit_form) }}
    {% include 'person/form_controls.html.twig' with {'btnLabel': 'Update', 'cancelPath': path('person_show', { 'id': person.id }) } %}
    {{ form_end(edit_form) }}

{% endblock %}
