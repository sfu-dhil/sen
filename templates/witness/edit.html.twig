{% extends 'base.html.twig' %}

{% block title %}Witness Edit{% endblock %}

{% block body %}
    <h1>Witness Edit</h1>

    {{ form_start(edit_form) }}
    {{ form_widget(edit_form) }}
    {% include 'witness/form_controls.html.twig' with {'btnLabel': 'Update', 'cancelPath': path('witness_show', { 'id': witness.id }) } %}
    {{ form_end(edit_form) }}

{% endblock %}
