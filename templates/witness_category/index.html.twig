{% extends 'base.html.twig' %}

{% block title %}Witness Category List{% endblock %}

{% block pageheader %}
    <h1>Witness Category List</h1>
    <p class="count">
        Displaying {{ witness_categories|length }} witness categories of {{ witness_categories.getTotalItemCount }}
        total.
    </p>
{% endblock %}

{% block body %}

    <div class='btn-toolbar pull-right'>
        <div class='btn-group'>
            {% if is_granted('ROLE_CONTENT_ADMIN') %}
                <a href="{{ path('witness_category_new') }}" class="btn btn-default">
                    <span class="glyphicon glyphicon-plus"></span> New </a>
            {% endif %}

            <a href="{{ path('witness_category_search') }}" class="btn btn-default">
                <span class="glyphicon glyphicon-search"></span> Search </a>
        </div>
    </div>

    {% embed 'witness_category/partial/table.html.twig' %}
    {% endembed %}

    <div class="navigation">
        {{ knp_pagination_render(witness_categories) }}
    </div>

{% endblock %}
